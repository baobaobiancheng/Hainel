# 代码审计记录：空壳 / TODO / 未调用路径（第 1 批）

## 一、检查范围

本次已覆盖：

- `backend/app/tasks/`：`reminder_tasks.py`、`notification_tasks.py`、`training_tasks.py`
- `backend/app/tasks/celery_app.py`（Beat 调度、include、debug_task）
- `backend/app/api/v1/__init__.py`（`setup_routes` 注册表）
- `backend/app/main.py`、`backend/main_dev.py`（入口与路由差异）
- `backend/app/api/v1/consultation.py`、`long_polling.py`（与通知链路的交叉核对）
- `backend/app/api/v1/messages.py`、`agents.py`（对 `consultation_service`、训练入口的引用方式）
- `frontend-doctor/src`：关键字 `TODO` / `FIXME` 等（全目录 grep）

**未覆盖（留作后续批次）：**

- `backend/app/agents/**`（逐子模块扫 `pass`、半实现、未接线分支）
- `backend/app/services/**`（与 API 脱节的服务）
- 前端路由与 `api` 模块相对 OpenAPI 的对照
- 全仓库其它目录（如 `资料/`、`scripts/`）未做系统性空壳扫描

## 二、方法与限制

- 以 **grep / 读文件** 为主：在 `backend/app` 内检索 `TODO`、`FIXME`、`.delay(`、对 `tasks` 模块的 import、路由 `include_router`。
- **「永远不会被调用」** 指：在当前仓库业务代码路径下未发现引用；不排除通过 Celery CLI、运维脚本、外部服务按任务名投递。
- 未运行 `pytest` / 静态分析工具做全量死代码证明。

## 三、结果摘要

### 3.1 明确标注的 TODO（实现未完成）

| 文件 | 说明 |
|------|------|
| `backend/app/tasks/reminder_tasks.py` | `check_and_send_reminders`、`send_reminder`、`create_recurring_reminders` 内为 TODO 占位说明 |
| `backend/app/tasks/notification_tasks.py` | `cleanup_expired_conversations`、`archive_old_medical_records` 内为 TODO 占位说明 |

### 3.2 空壳（会执行但几乎无业务逻辑）

- 上述带 TODO 的 Celery 任务体：**多为打日志 + 返回 success**，核心业务（查库、发通知、更新状态、归档等）未写。
- **注意**：其中 `check_and_send_reminders`、`cleanup_expired_conversations`、`archive_old_medical_records` 已被 **Celery Beat 定时调度**，因此会周期性执行，但**不产生设计文档所述的实际副作用**。

### 3.3 当前业务代码中未见调用链的路径

（在 `backend/app` 内未发现 API/services 对以下任务的 `.delay` / `apply_async` 或 import 触发）

| 类别 | 对象 |
|------|------|
| Celery 训练 | `train_agent`、`train_agents_batch`、`evaluate_agent_performance`、`update_agent_rewards`（HTTP 训练走 `agents.py` + 同步 `RLTrainer`，且 Beat 中批量训练调度为注释） |
| Celery 通知 | `notification_tasks.send_message_notification`、`send_conversation_status_notification`、`send_batch_notifications`（业务侧使用 `long_polling.send_message_notification` 异步直推轮询，与 Celery 任务**同名不同符号**） |
| Celery 提醒 | `send_reminder`、`create_recurring_reminders`（无 Beat、无 app 内 `.delay`） |
| 其它 | `celery_app.debug_task`（无调度与引用） |

### 3.4 路由/入口差异（易误判为「死代码」）

- `app/api/v1/consultation.py` 已定义 REST（如 `/consultation/diagnosis`），但 **`setup_routes()` 未注册该 router**。
- **`main_dev.py`** 单独 `include_router(consultation_router)`；**`app/main.py`** 仅 `setup_routes()`，故 **正式入口下该 REST 不会挂载**。诊断可走 `messages` + `consultation_service`，该 REST 更接近备用/开发入口。

### 3.5 前端 `frontend-doctor/src`

- 未发现 `TODO` / `FIXME` 等关键字匹配（不代表无技术债）。
- `RLTraining.vue` 中 `formatMetrics` 对非数字指标显示为 `--` 属展示问题，已单独说明，不记入「空壳模块」。

### 3.6 非死代码说明

- `DatabaseTask.run` 中 `raise NotImplementedError` 为 Celery 任务基类约定，由具体 task 覆盖，**属正常模式**。

## 四、后续建议

1. 若产品依赖定时提醒/清理/归档：补齐 `reminder_tasks` / `notification_tasks` 中 TODO，或关闭 Beat 对应项避免「假成功」。
2. 若需统一异步通知：明确只用 `long_polling` 直推或 Celery 队列其一，避免两套同名语义。
3. 若生产使用 `app.main`：在 `setup_routes()` 中注册 `consultation` 或删除/文档化 `main_dev` 专用路由。
4. 第 2 批优先：`backend/app/agents/**` 与 `backend/app/services/**`。

---
*记录日期：2026-05-02；与对话中「第 1 批」审计结论一致。*
