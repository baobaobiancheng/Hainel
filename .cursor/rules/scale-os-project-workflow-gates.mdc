---
description: SCALE OS 项目级流程门禁与红线（与系统提示词分离）
alwaysApply: true
---

# SCALE OS 项目级流程门禁与红线

该规则来源于 `scale-os-config-cursor/.cursorrules` 与 `.scale/workflow.json` 的稳定部分，仅保留长期可执行约束，不引入运行时状态。

## Workflow（standard）
- step_1 探索：读文档 + 扫代码 + 找技能 + 矛盾分析
- step_2 规划：需求精炼 + 影响分析 + 契约定义
- step_3 执行：TDD（RED→GREEN→REFACTOR），禁止同时写代码和测试
- step_4 验证：lint / test / typecheck 通过，结论必须基于工具输出
- step_5 沉淀：泛化检查 + 经验文档化 + AI Slop 自检

## Gates
- G1: 探索完成 → 已读文件数 >= 3
- G2: 规划完成 → 计划文档含功能边界 + 异常契约 + 回滚方案
- G3: TDD 合规 → 测试文件先于实现文件存在
- G4: Lint 通过 → `ruff check .` exit code = 0
- G5: 测试通过 → `pytest` exit code = 0

## Tier Flow Reference
- express: `fix -> verify`
- standard: `plan -> execute -> verify`
- full: `explore -> plan -> execute -> verify -> consolidate`
- critical: `explore -> plan -> review -> execute -> verify -> security -> consolidate`

## Red Lines
- R1: 零数据丢失 → migration 必须有 down 方法
- R2: 零静默失败 → 禁止空 catch 块
- R3: 零硬编码密钥 → 敏感信息仅可通过环境变量传入
- R4: 零幻觉 → 不确定时标记 `[UNCERTAIN]`
- R5: 零甩锅 → 声称“环境问题”前必须完成验证
