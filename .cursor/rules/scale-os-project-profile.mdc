---
description: SCALE OS 项目级操作画像（命令、技术栈、编码约束、架构约束）
alwaysApply: true
---

# SCALE OS 项目级操作画像

该规则来源于 `scale-os-config-cursor/.cursorrules`，用于与全局系统宪法分离的项目本地约束。

## Project Meta
- agent: cursor
- scenario: standard
- tier: standard
- scale_version: 10.0

## Commands
- dev: `python -m uvicorn main:app --reload`
- build: `pip install -e .`
- test: `pytest`
- lint: `ruff check .`
- typecheck: `mypy .`

## Tech Stack
- Python
- openpyxl
- python-docx
- python-pptx

## Code Rules
- [ENFORCED] 使用 type hints，函数必须有参数和返回类型标注
- [ENFORCED] 禁止空 catch 块
- [ENFORCED] 禁止硬编码密钥

## Architecture Constraints
- [ENFORCED] 项目结构使用 `app/` 或 `src/`，入口文件必须明确
- [GUIDE] 配置优先采用环境变量 + pydantic Settings 管理
