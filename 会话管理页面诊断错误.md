问题：
会话管理页面，点击会话的诊断按钮，会跳转的辅助诊断页面，显示报错：
1.请求资源不存在
2.加载诊断数据失败
帮我查一下是什么原因


检查：
1.「请求资源不存在」
来自医生端全局请求封装里对 HTTP 404 的处理：

        case 404:
          message.error('请求资源不存在')
          break
说明浏览器请求的某个接口路径，后端返回了 404 Not Found。

2.「加载诊断数据失败」
辅助诊断页在 loadDiagnosisData 里只要接口抛错就会再提示一次：

const loadDiagnosisData = async () => {
  if (!selectedConversationId.value) return
  loading.value = true
  try {
    const suggestionsRes = await getDiagnosisSuggestions(selectedConversationId.value)
    ...
    const analysisRes = await getAgentAnalysis(selectedConversationId.value)
    ...
  } catch (error) {
    message.error('加载诊断数据失败')
  } finally {
3. 根因：后端没有实现这些接口
前端调的是：

export const getDiagnosisSuggestions = (conversationId) => {
  return request({
    url: `/v1/diagnosis/suggestions`,
    method: 'get',
    params: { conversation_id: conversationId }
  })
}
export const getAgentAnalysis = (conversationId) => {
  return request({
    url: `/v1/diagnosis/agent_analysis`,
    method: 'get',
    params: { conversation_id: conversationId }
  })
}
在 baseURL: '/api' 下，实际请求的是 GET /api/v1/diagnosis/suggestions 和 GET /api/v1/diagnosis/agent_analysis。

而后端 setup_routes() 只注册了 auth、conversations、messages 等，没有注册任何 diagnosis 路由，app/api/v1/ 下也没有 diagnosis.py：

def setup_routes() -> None:
    """注册全部 API 路由（供 main.py 调用）"""
    from app.api.v1 import (
        auth,
        conversations,
        messages,
        medical_records,
        reports,
        long_polling,
        reminders,
        knowledge,
        agents,
        doctors,
    )
    ...
    api_router.include_router(doctors.router)
因此从会话页点「诊断」跳转到 /diagnosis?conversation_id=... 后，页面会按 conversation_id 去请求上述两个地址，服务器上没有对应路由 → 404 → 先弹「请求资源不存在」，再被 catch 弹「加载诊断数据失败」。

会话页的跳转本身是正常的（例如 viewDiagnosis 使用 conversation_id 查询参数），问题在前后端接口未对齐：前端写了诊断 API，后端未实现。

若你需要修复，下一步是在后端增加 diagnosis 路由模块并在 setup_routes 中 include_router，实现与 diagnosis.js 一致的三个端点（含 submit），或改为从已有接口（例如会话消息里的 extra_metadata）组装诊断数据并在前端改调这些接口。需要我直接在仓库里补后端实现的话，可以说一下你更希望「新建诊断 API」还是「复用消息/会话里的 AI 元数据」。

