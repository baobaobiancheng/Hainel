# PDF文档识别功能实现计划

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现PDF文档识别功能，支持患者上传PDF格式检查报告并通过现有DashScope OCR API进行文字、表格、版式识别

**Architecture:** 使用pdf2image将PDF转换为图片，复用现有的DashScopeOCRService（qwen-vl-plus）进行识别

**Tech Stack:** FastAPI, pdf2image, PIL, DashScopeOCRService

---

## 文件结构

```
backend/
├── app/
│   ├── ai/
│   │   └── ocr/
│   │       ├── base.py                    # OCR服务基类
│   │       └── dashscope_ocr.py            # DashScope OCR实现
│   ├── api/
│   │   └── v1/
│   │       └── reports.py                 # 报告API（需修改）
│   └── utils/
│       └── pdf_processor.py               # 新建：PDF处理工具类
├── requirements.txt                        # 需添加依赖
└── tests/
    └── unit/
        └── test_pdf_processor.py          # 新建：单元测试
```

---

## Chunk 1: 依赖安装与PDF处理工具类

### Task 1: 添加pdf2image依赖

**Files:**
- Modify: `backend/requirements.txt`

- [ ] **Step 1: 添加pdf2image依赖**

在requirements.txt中添加：
```
# PDF处理
pdf2image>=1.17.0
```

- [ ] **Step 2: Commit**

```bash
git add backend/requirements.txt
git commit -m "chore: add pdf2image dependency for PDF processing"
```

---

### Task 2: 创建PDF处理工具类

**Files:**
- Create: `backend/app/utils/pdf_processor.py`
- Test: `backend/tests/unit/test_pdf_processor.py`

- [ ] **Step 1: 创建PDF处理工具类**

```python
"""
PDF处理工具类
将PDF文档转换为图片，供OCR识别使用
"""
import io
from typing import List, Union
from pathlib import Path
from PIL import Image
import logging

logger = logging.getLogger(__name__)

# 尝试导入pdf2image
try:
    from pdf2image import convert_from_bytes, convert_from_path
    PDF2IMAGE_AVAILABLE = True
except ImportError:
    PDF2IMAGE_AVAILABLE = False
    logger.warning("pdf2image未安装，PDF转图片功能不可用")


class PDFProcessorError(Exception):
    """PDF处理异常"""
    pass


class PDFProcessor:
    """PDF处理器 - 将PDF转换为PIL Image列表"""

    @staticmethod
    def is_available() -> bool:
        """检查PDF处理是否可用"""
        return PDF2IMAGE_AVAILABLE

    @staticmethod
    def ensure_available():
        """确保PDF处理可用，否则抛出异常"""
        if not PDF2IMAGE_AVAILABLE:
            raise PDFProcessorError(
                "pdf2image库未安装，请运行: pip install pdf2image"
            )

    @staticmethod
    def convert_from_bytes(pdf_bytes: bytes, dpi: int = 200) -> List[Image.Image]:
        """
        从字节流转换PDF为图片列表

        Args:
            pdf_bytes: PDF文件的字节内容
            dpi: 输出图片的分辨率，默认200

        Returns:
            List[Image.Image]: 每一页对应的PIL Image对象列表

        Raises:
            PDFProcessorError: pdf2image未安装或转换失败
        """
        PDFProcessor.ensure_available()

        try:
            images = convert_from_bytes(pdf_bytes, dpi=dpi)
            logger.info(f"PDF转换成功，共 {len(images)} 页")
            return images
        except Exception as e:
            logger.error(f"PDF转换失败: {e}")
            raise PDFProcessorError(f"PDF转换失败: {e}")

    @staticmethod
    def convert_from_file(file_path: str, dpi: int = 200) -> List[Image.Image]:
        """
        从文件路径转换PDF为图片列表

        Args:
            file_path: PDF文件的本地路径
            dpi: 输出图片的分辨率，默认200

        Returns:
            List[Image.Image]: 每一页对应的PIL Image对象列表

        Raises:
            PDFProcessorError: pdf2image未安装或转换失败
        """
        PDFProcessor.ensure_available()

        try:
            images = convert_from_path(file_path, dpi=dpi)
            logger.info(f"PDF转换成功，共 {len(images)} 页")
            return images
        except Exception as e:
            logger.error(f"PDF转换失败: {e}")
            raise PDFProcessorError(f"PDF转换失败: {e}")

    @staticmethod
    def get_page_count(pdf_bytes: bytes) -> int:
        """
        获取PDF页数

        Args:
            pdf_bytes: PDF文件的字节内容

        Returns:
            int: PDF页数
        """
        # 使用pypdf获取页数（轻量级）
        try:
            from pypdf import PdfReader
            reader = PdfReader(io.BytesIO(pdf_bytes))
            return len(reader.pages)
        except Exception:
            # 如果pypdf不可用，尝试pdf2image
            images = PDFProcessor.convert_from_bytes(pdf_bytes, dpi=72)
            return len(images)
```

- [ ] **Step 2: 运行测试验证（可选）**

由于需要实际的PDF文件，此步骤跳过

- [ ] **Step 3: Commit**

```bash
git add backend/app/utils/pdf_processor.py
git commit -m "feat: add PDF processing utility class"
```

---

## Chunk 2: 扩展OCR服务支持PDF识别

### Task 3: 扩展DashScopeOCRService支持PDF识别

**Files:**
- Modify: `backend/app/ai/ocr/dashscope_ocr.py`

- [ ] **Step 1: 添加recognize_pdf方法**

在`DashScopeOCRService`类中添加以下方法（文件末尾添加）：

```python
    def recognize_pdf(
        self,
        pdf_bytes: bytes,
        dpi: int = 200,
        **kwargs
    ) -> OCRResult:
        """
        识别PDF文档中的文字

        Args:
            pdf_bytes: PDF文件的字节内容
            dpi: 转换为图片时的分辨率，默认200

        Returns:
            OCRResult: OCR识别结果，包含多页合并后的文本
        """
        from app.utils.pdf_processor import PDFProcessor, PDFProcessorError

        try:
            # 将PDF转换为图片
            images = PDFProcessor.convert_from_bytes(pdf_bytes, dpi=dpi)
            logger.info(f"开始OCR识别PDF文档，共 {len(images)} 页")

            # 逐页识别
            page_results = []
            for i, image in enumerate(images):
                try:
                    result = self._call_api(image)
                    page_text = result.text
                    page_results.append({
                        "page": i + 1,
                        "text": page_text,
                        "confidence": result.confidence,
                    })
                    logger.info(f"第 {i+1}/{len(images)} 页识别完成")
                except Exception as e:
                    logger.error(f"第 {i+1} 页识别失败: {e}")
                    page_results.append({
                        "page": i + 1,
                        "text": "",
                        "confidence": 0.0,
                        "error": str(e),
                    })

            # 合并结果
            full_text = ""
            confidences = []
            for pr in page_results:
                if pr["text"]:
                    full_text += f"--- 第 {pr['page']} 页 ---\n{pr['text']}\n\n"
                    confidences.append(pr["confidence"])

            avg_confidence = sum(confidences) / len(confidences) if confidences else 0.0

            return OCRResult(
                text=full_text.strip(),
                confidence=avg_confidence,
                metadata={
                    "model": self.model,
                    "page_count": len(images),
                    "page_results": page_results,
                },
            )

        except PDFProcessorError as e:
            logger.error(f"PDF处理失败: {e}")
            raise
        except Exception as e:
            logger.error(f"PDF OCR识别失败: {e}", exc_info=True)
            raise
```

- [ ] **Step 2: Commit**

```bash
git add backend/app/ai/ocr/dashscope_ocr.py
git commit -m "feat: add recognize_pdf method to DashScopeOCRService"
```

---

## Chunk 3: 更新报告API支持PDF OCR

### Task 4: 更新reports.py中的PDF OCR逻辑

**Files:**
- Modify: `backend/app/api/v1/reports.py:104-127`

- [ ] **Step 1: 修改upload_report函数中的PDF处理逻辑**

找到现有的PDF处理代码块（约第121-127行），替换为：

```python
                if file_ext in ["jpg", "jpeg", "png", "bmp"]:
                    # 图片文件，直接OCR识别
                    ocr_service = get_ocr_service()
                    ocr_result = ocr_service.recognize_bytes(file_content)
                    ocr_text = ocr_result.text
                elif file_ext == "pdf":
                    # PDF文件，转换为图片后OCR识别
                    from app.utils.pdf_processor import PDFProcessor
                    if not PDFProcessor.is_available():
                        logger.warning("pdf2image未安装，PDF OCR功能不可用")
                        ocr_text = ""
                    else:
                        try:
                            ocr_service = get_ocr_service()
                            ocr_result = ocr_service.recognize_pdf(file_content)
                            ocr_text = ocr_result.text
                            # 将页数信息保存到metadata
                            page_count = ocr_result.metadata.get("page_count", 1) if ocr_result.metadata else 1
                            logger.info(f"PDF OCR识别完成，共 {page_count} 页")
                        except Exception as e:
                            logger.error(f"PDF OCR识别失败: {e}")
                            ocr_text = ""
                else:
                    logger.warning(f"不支持的文件类型进行OCR: {file_ext}")
                    ocr_text = ""
```

- [ ] **Step 2: 更新metadata中保存页数信息**

在创建消息的metadata部分（约第142-148行），添加page_count信息：

```python
            metadata={
                "ocr_result": {
                    "text": ocr_text,
                    "confidence": ocr_result.confidence if ocr_result else 0.0,
                    "page_count": ocr_result.metadata.get("page_count", 1) if ocr_result and ocr_result.metadata else None,
                } if ocr_result else None,
                "auto_ocr": auto_ocr,
                "file_type": file_ext,
            },
```

- [ ] **Step 3: Commit**

```bash
git add backend/app/api/v1/reports.py
git commit -m "feat: add PDF OCR support in reports upload API"
```

---

## Chunk 4: 添加pypdf依赖（可选，用于获取页数）

### Task 5: 添加pypdf依赖

**Files:**
- Modify: `backend/requirements.txt`

- [ ] **Step 1: 添加pypdf依赖**

在requirements.txt中添加：
```
# PDF处理
pypdf>=3.0.0
```

- [ ] **Step 2: Commit**

```bash
git add backend/requirements.txt
git commit -m "chore: add pypdf dependency for PDF page count"
```

---

## 验证与测试

### 测试场景

1. **上传PDF文件**：患者上传一个3页的PDF体检报告
2. **自动OCR**：系统自动将PDF转为图片并识别
3. **结果验证**：检查返回的OCR结果是否包含3页的内容

### 手动测试命令

```bash
# 启动服务
cd backend
uvicorn app.main:app --reload

# 测试上传PDF（使用curl）
curl -X POST "http://localhost:8000/api/v1/reports/upload" \
  -F "conversation_id=1" \
  -F "file=@/path/to/report.pdf" \
  -F "auto_ocr=true" \
  -H "Authorization: Bearer <token>"
```

---

## 实现总结

| Task | 描述 | 状态 |
|------|------|------|
| 1 | 添加pdf2image依赖 | ⬜ |
| 2 | 创建PDF处理工具类 | ⬜ |
| 3 | 扩展OCR服务支持PDF | ⬜ |
| 4 | 更新reports.py API | ⬜ |
| 5 | 添加pypdf依赖 | ⬜ |
